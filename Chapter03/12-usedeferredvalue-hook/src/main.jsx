import { createRoot } from "react-dom/client";
import SearchInputWithDeferredResults from "./SearchInputWithDeferredResults";

createRoot(document.getElementById("root")).render(
  <SearchInputWithDeferredResults />
);
